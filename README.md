# CodaPayments
The solution includes project using which we can load a .csv file and convert individual record into a text file that contains data in form of xml and store in the disk
The UI is created using .net winforms controls
